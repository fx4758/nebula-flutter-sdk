name: nebula_sdk
description: Shared client foundation for Nebula multi-app Flutter products.
version: 0.1.0-rc1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies: {}

dev_dependencies:
  # 官方测试框架（纯 Dart，无平台 plugin；仅 dev，不进入发布产物）。
  test: ^1.25.0

