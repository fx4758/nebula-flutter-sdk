/// Canonical mobile installation-bootstrap endpoint (Bootstrap Contract V2).
library;

/// Frozen endpoint paths for installation bootstrap.
final class BootstrapEndpoints {
  const BootstrapEndpoints._();

  /// Public pre-installation bootstrap route.
  static const String bootstrap = '/api/v1/mobile/bootstrap';
}
